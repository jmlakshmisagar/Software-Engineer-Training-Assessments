package streams.api;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class DemoOnStreams {

	public static void main(String[] args) {
//		Integer arr[] = { 9, 8, 7, 6, 5, 4, 3, 2, 1, 5, 6, 9, 8, 7, 1, 2, 4 };
//		List<Integer> integers = Arrays.stream(arr).forEach(System.out::println);

//		List<Integer> integers = Arrays.stream(arr).collect(Collectors.toList());
//		System.out.println(integers);

//		System.out.println(Arrays.stream(arr).min((a, b) -> a > b ? 1 : -1).get());
//		System.out.println(Arrays.stream(arr).min(Integer::compare).get());

//		System.out.println(Arrays.stream(arr).count());

//		int sum = Arrays.stream(arr).reduce(0, (a, b) -> a + b);
//		System.out.println(sum);

//		int sum1 = Arrays.stream(arr).reduce(0, (a, b) -> a + b);

//		Stream.of(arr).filter(n -> n % 2 == 0).forEach(System.out::println);

//		Stream.of(arr).map(a -> a + 10).forEach(System.out::println);
//		Stream.of(arr).limit(4).forEach(System.out::println);
//		System.out.println(Stream.of(arr).distinct().count());

//		System.out.println(Arrays.toString(arr));

//		Stream.of(arr).filter(a -> a % 3 == 0).sorted().forEach(System.out::println);
//		Stream.of(arr).sorted((a, b) -> a > b ? -1 : 1).forEach(System.out::println);

//		String[] names = { "mphasis", "rvce", "mca", "abcd", "terminal", "sarasvathi", "swathi", "swayam" };
//		Arrays.stream(names).filter(a -> a.length() >= 5 && a.startsWith("s")).sorted(Comparator.reverseOrder())
//				.forEach(System.out::println); 
		
		

	}

}
